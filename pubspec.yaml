name: windows_system_info
description: A Flutter package for getting exclusive details of windows device.
version: 0.0.2
repository: https://github.com/Abdulazeezvp/windows_system_info

platforms:
  windows:

environment:
  sdk: ">=2.17.3 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
